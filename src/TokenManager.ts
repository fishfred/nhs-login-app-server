class TokenManager {
    constructor(){
        // connect to database
    }

    generateToken(userid: string){
        // Generate a jwt and return it
    }

    verifyToken(token: string): boolean {
        return false;
    }
}